{
  "name": "agentpay-rsk",
  "version": "1.0.0",
  "description": "Freelance escrow dApp on Rootstock",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network rskTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network rskMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
